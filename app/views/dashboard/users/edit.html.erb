<h1>Edit <%= @user.email %></h1>
<h2>
  <%= link_to 'Activate English Language', current_language_path(current_language: 'en'), method: :post %>
</h2>
<h2>
  <%= link_to 'Activate Russian Language', current_language_path(current_language: 'ru'), method: :post %>
</h2>
<%= simple_form_for @user, url: { controller: :users, action: :update } do |f| %>
<% if @user.errors.any? %>
  <h2>
    <%= pluralize(@user.errors.count, 'error')%> prohibited this card from being created.
  </h2>
  <% @user.errors.full_messages.each do |error| %>
  <ul>
    <li><%= error %>
  </ul>
  <% end %>
<% end %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.button :submit %>
<% end %>
